{
  "installCommand": "cd .. && npm ci",
  "buildCommand": "npm run build",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- umltutor-frontend/ package-lock.json",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
