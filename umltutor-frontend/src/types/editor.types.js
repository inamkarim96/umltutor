



/**
 * Enum representing the three steps in the UML editor workflow
 */
export var EditorStep; (function (EditorStep) {
    const DIAGRAM = 'DIAGRAM'; EditorStep["DIAGRAM"] = DIAGRAM;
    const DESCRIPTION = 'DESCRIPTION'; EditorStep["DESCRIPTION"] = DESCRIPTION;
    const SSD = 'SSD'; EditorStep["SSD"] = SSD;
    const SD = 'SD'; EditorStep["SD"] = SD; // Sequence Diagram (full)
})(EditorStep || (EditorStep = {}));

/**
 * Status of a workflow step
 */
 





















































